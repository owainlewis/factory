# Public content or tutorial repo.

extends: default

requires:
  content:
    - README explains how content is organized.
    - Index is current.
    - Each tutorial has source or video context when applicable.
  checks:
    - links are checked or tracked.
    - examples have smoke tests when practical.
  routing:
    - YouTube, newsletter, or product CTA is current when public.

