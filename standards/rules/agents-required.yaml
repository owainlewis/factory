id: agents-required
name: AGENTS.md required
severity: warning
applies_to:
  active: true
check:
  source: repo
  file: AGENTS.md
  condition: exists
fix:
  mode: pull_request
  safe_auto_fix: true
  guidance: Add a short AGENTS.md with repo purpose, commands, and safety rules.
human_review: []

