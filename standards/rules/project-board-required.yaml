id: project-board-required
name: GitHub Project board required
severity: warning
applies_to:
  active: true
check:
  source: github
  field: repository.projectsV2
  condition: present
fix:
  mode: issue
  safe_auto_fix: false
  guidance: Create or request a repo project board, then link open issues.
human_review:
  - project_tracking

