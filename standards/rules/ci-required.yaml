id: ci-required
name: CI required
severity: warning
applies_to:
  active: true
check:
  source: repo
  path: .github/workflows
  condition: has_files
fix:
  mode: issue
  safe_auto_fix: false
  guidance: Open an issue naming the expected checks from the standards profile.
human_review:
  - automation_changes

