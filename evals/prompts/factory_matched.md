Read the task and inspect the existing code. Make a short plan, implement the
change, and add appropriate tests. Run the visible checks and fix any failures.
Address any feedback supplied from the prior review without weakening tests.

The Factory runner invokes the independent reviewer after you finish. Leave that
review invocation to the runner. Finish with a concise summary of your changes.
