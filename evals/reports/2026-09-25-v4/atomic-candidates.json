{
  "atomic-save--factory_matched--1": "import json\nimport os\nimport stat\nimport tempfile\n\ndef _umask():\n    current = os.umask(0)\n    os.umask(current)\n    return current\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory)\n    try:\n        with os.fdopen(fd, 'w') as f:\n            json.dump(data, f)\n        try:\n            mode = os.stat(path).st_mode\n        except OSError:\n            mode = 0o666 & ~_umask()\n        os.chmod(tmp_path, stat.S_IMODE(mode))\n        os.replace(tmp_path, path)\n    except Exception:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n",
  "atomic-save--factory_matched--2": "import json\nimport os\nimport tempfile\n\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory, prefix=os.path.basename(path) + '.', suffix='.tmp')\n    try:\n        try:\n            mode = os.stat(path).st_mode & 0o777\n        except OSError:\n            umask = os.umask(0)\n            os.umask(umask)\n            mode = 0o666 & ~umask\n        os.chmod(tmp_path, mode)\n        with os.fdopen(fd, 'w') as f:\n            json.dump(data, f)\n            f.flush()\n            os.fsync(f.fileno())\n        os.replace(tmp_path, path)\n    except BaseException:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n",
  "atomic-save--factory_verified--1": "import json\nimport os\nimport stat\nimport tempfile\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory)\n    try:\n        try:\n            mode = stat.S_IMODE(os.stat(path).st_mode)\n        except OSError:\n            umask = os.umask(0)\n            os.umask(umask)\n            mode = 0o666 & ~umask\n        os.chmod(tmp_path, mode)\n        with os.fdopen(fd, 'w') as f:\n            json.dump(data, f)\n        os.replace(tmp_path, path)\n    except Exception:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n",
  "atomic-save--factory_verified--2": "import json\nimport os\nimport tempfile\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory)\n    try:\n        with os.fdopen(fd, 'w') as f:\n            json.dump(data, f)\n        try:\n            mode = os.stat(path).st_mode\n        except OSError:\n            pass\n        else:\n            os.chmod(tmp_path, mode)\n        os.replace(tmp_path, path)\n    except Exception:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n",
  "atomic-save--subagent_verified--1": "import json\nimport os\nimport stat\nimport tempfile\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory)\n    try:\n        try:\n            mode = stat.S_IMODE(os.stat(path).st_mode)\n        except OSError:\n            umask = os.umask(0)\n            os.umask(umask)\n            mode = 0o666 & ~umask\n        os.chmod(tmp_path, mode)\n        with os.fdopen(fd, 'w') as f:\n            json.dump(data, f)\n            f.flush()\n            os.fsync(f.fileno())\n        os.replace(tmp_path, path)\n    except BaseException:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n",
  "atomic-save--subagent_verified--2": "import json\nimport os\nimport tempfile\n\ndef save(path, data):\n    path = os.fspath(path)\n    directory = os.path.dirname(path) or '.'\n    fd, tmp_path = tempfile.mkstemp(dir=directory, prefix='.tmp-', suffix='.json')\n    try:\n        with os.fdopen(fd, 'w') as f:\n            umask = os.umask(0)\n            os.umask(umask)\n            os.fchmod(f.fileno(), 0o666 & ~umask)\n            json.dump(data, f)\n        os.replace(tmp_path, path)\n    except Exception:\n        try:\n            os.remove(tmp_path)\n        except OSError:\n            pass\n        raise\n\ndef load(path):\n    with open(path) as f:\n        return json.load(f)\n"
}
